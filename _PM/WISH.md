請移除"md"與"對話格式"
只保留純文字的小說即可
然後我希望產出的內容可以直接在畫面上進行"搜尋"與"取代"，讓我可以快速將錯誤的名稱修正好
移除所有下載按鈕下載流程，直接手動全選複製即可




這是一個非常實用的需求！既然您主要是為了後續的校稿與修正，我們可以把原先的「純觀看模式」改造成「可編輯的純文字校稿層」。

為了滿足「只保留純文字」以及「可以在畫面上搜尋與取代」的需求，以下是接下來的修改計畫：

🛠️ 修改計畫 (Plan)
1. 精簡輸出格式與 Prompt
調整 AI 提示詞：告訴 Gemini 不再需要加上 Markdown 的標記符號（如 ** 或 ##），直接產出乾淨、排版過的「純文字小說」即可。
清理多餘變數：移除程式碼中關於 

markdown
 與 dialogue 的變數與過濾邏輯，專心處理唯一的一份純文字結果。
2. 大幅簡化結果介面 (UI)
移除分頁系統：將原本的「Markdown / 純文字 / 對話格式」分頁按鈕全部移除。
引入編輯器 (Textarea)：將原本只能觀看的 <pre> 區塊變成一個大型的文字輸入框 <textarea>，這樣您甚至可以直接手動微調錯字。
清理下載按鈕：移除「下載 Markdown (.md)」與「下載對話格式 (.txt)」。保留「下載純文字 (.txt)」以及修改過的「下載 Word (.docx)」（直接把框內的純文字輸出為 Word 檔）。
3. 新增「搜尋與取代」工具列 (Search & Replace)
會在文字編輯區上方加上一排簡潔的工具：
🔍 搜尋框 (Search)
✏️ 取代框 (Replace)
按鈕：「找下一個」、「取代」、「全部取代」
功能邏輯 (JS)：
當點擊「找下一個」時，程式會自動尋找該字尾並反白（選中）文字段落，畫面隨之捲動。
當點擊「取代 / 全部取代」時，會直接置換編輯器內的文字，讓您快速批次修正 Gemini 偶爾辨識錯的玩家名稱或專有名詞。